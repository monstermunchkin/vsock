# vsock

```
$ vsock-server -h
Usage of vsock-server:
  -port uint
    	Port to listen on (default 1234)
```

```
$ vsock-client -h
Usage of vsock-client:
  -context uint
    	Context ID (default 3)
  -port uint
    	Port to connect to (default 1234)
```
